version: '3.8' # refers docker file syntax version

services:
  dynamic-model-api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app  # optional: only if you want live reload on code changes
    environment:
      - FLASK_ENV=development  # optional: for debug mode if needed
